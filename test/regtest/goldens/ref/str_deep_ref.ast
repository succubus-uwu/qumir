(block
  (fun f () -> string
    (block
      (var знач = (bitcast 0 string))
      (var s = (bitcast 0 string))
      (call инит s)
      (replace знач (retain (borrow s)))
      (return знач)))
  (fun инит ((var s <ref <string (mutable)>>))
    (block
      (call deep s)
      (return)))
  (fun deep ((var s <ref <string (mutable)>>))
    (block
      (replace s (own-literal "abc"))
      (return))))