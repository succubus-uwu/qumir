(block
  (fun функция () -> string
    (block
      (var знач = (bitcast 0 string))
      (replace знач (own-literal "hello"))
      (if (cast #t bool) (block (return знач)))
      (replace знач (own-literal "unreached"))
      (return знач))))